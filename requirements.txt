# requirements.txt — v3
# Instalar con: pip install -r requirements.txt

# ── Telegram ──
python-telegram-bot[job-queue]==21.5

# ── APIs de datos ──
requests>=2.32.5
# understatapi eliminado: pina idna==2.10 que conflictúa con soccerdata/seleniumbase
# El pipeline cae automáticamente a KambiConsensus cuando no está disponible
soccerdata>=1.9.0

# ── Análisis de datos ──
pandas==2.1.4
numpy==1.26.3
scipy==1.11.4

# ── Scraping (Rushbet) ──
playwright==1.41.2
# Render build: playwright install chromium (ver render.yaml)

# ── Google Sheets (base de datos) ──
gspread==6.0.2
google-auth==2.27.0

# ── Imágenes ──
Pillow==10.2.0

# ── Variables de entorno ──
python-dotenv==1.0.1

# ── Zona horaria ──
pytz==2024.1
tzdata==2025.3

# ── TikTok — generación de videos ──
moviepy==1.0.3
imageio-ffmpeg==0.5.1
gTTS==2.5.1
httpx==0.27.0